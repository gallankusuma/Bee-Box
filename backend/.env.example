DATABASE_URL="file:./dev.db"
JWT_ACCESS_SECRET="change-me-access-secret"
JWT_REFRESH_SECRET="change-me-refresh-secret"
PORT=4000

# Comma-separated browser origins allowed to call this API (CORS). Add your
# deployed student/teacher web URLs here in production.
ALLOWED_ORIGINS="http://localhost:8090,http://localhost:8091,https://localhost"

// Per-deployment API endpoint. Edit this file (not app.js) when deploying to
// staging/production - it's loaded before app.js and nothing else reads
// process.env in this static site, so this is the one place to change.
window.BEE_BOX_API_BASE = 'http://localhost:4000/api';
